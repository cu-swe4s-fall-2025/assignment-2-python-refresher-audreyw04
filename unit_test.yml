name: Tests

on:
  push:
  pull_request:
    branches:
      - aew-v0

jobs:
  run_unit_tests:
    runs_on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 test/test_my_utils.py